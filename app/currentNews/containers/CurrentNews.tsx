import React from "react";
import CurrentNewsView from "../components/CurrentNewsView";

const CurrentNews = (): React.ReactElement => {
  return (
    <CurrentNewsView/>
  );
};

export default CurrentNews;